<!DOCTYPE html>
<html lang="en">
  <%- include("includes/head") %>
  <body>
    <h1>Dice gave value : <%=diceVal %></h1>
    <!-- writing if-else in ejs -->
    <% if(diceVal == 6){ %>
    <h1>Nice! Roll dice again...</h1>
    <%}%>
  </body>
</html>
